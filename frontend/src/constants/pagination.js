/**
 * Page-size constants. Kept separate per feature since each list is
 * paginated independently server-side, but centralized so the values
 * aren't magic numbers scattered across feature files.
 */
export const PORTFOLIO_PAGE_SIZE = 9;
export const MESSAGES_PAGE_SIZE = 10;
export const PROJECTS_ADMIN_PAGE_SIZE = 10;
